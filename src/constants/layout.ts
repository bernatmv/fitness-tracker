/**
 * Shared layout constants
 */

/** Height of the floating tab-bar pill (see AppNavigator). Screens with
 * scrolling content should pad their bottom by at least this much. */
export const TAB_PILL_HEIGHT = 64;
